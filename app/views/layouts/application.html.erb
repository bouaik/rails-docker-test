<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body style="margin: 0">
    <%# if !(Rails.env == "test") && request.original_url.split("https://")[1].split(".")[0] == "thawing-earth-00760" %>
      <h2 style="color:blue; background-color:yellow; text-align:center; padding: 20px">Préproduction</h2>
    <%# end %>

    <%= yield %>
  </body>
</html>
